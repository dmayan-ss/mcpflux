[project]
name = "mcflux"
version = "0.1.0"
description = "MCP server for InfluxDB v1 — read-only query and schema exploration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
]
